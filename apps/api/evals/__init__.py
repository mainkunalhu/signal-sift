"""Eval harness: task set (tasks.yaml) + scorer (scorer.py).

Run: uv run python -m evals.scorer [--limit N] [--category factual]
Results: docs/eval_results.json (also the source of the README metrics).
"""
